# Вклад в проект

Спасибо за интерес к проекту Telegram Bot с DeepSeek AI! Мы приветствуем вклад от сообщества.

## Как внести вклад

### 1. Подготовка

- Убедитесь, что у вас установлена Python 3.8+
- Клонируйте репозиторий
- Создайте виртуальное окружение
- Установите зависимости для разработки:

```bash
pip install -e ".[dev]"
```

### 2. Разработка

- Создайте новую ветку для ваших изменений: `git checkout -b feature/your-feature-name`
- Следуйте стилю кода проекта
- Добавляйте тесты для новых функций
- Обновляйте документацию при необходимости

### 3. Стиль кода

Проект использует следующие инструменты для поддержания качества кода:

```bash
# Форматирование
black .

# Проверка импортов
isort .

# Линтинг
flake8 main.py tests/

# Проверка типов
mypy main.py --ignore-missing-imports
```

### 4. Тестирование

- Запускайте все тесты перед отправкой изменений:

```bash
pytest tests/
```

- Убедитесь, что покрытие кода не уменьшилось:

```bash
pytest --cov=main tests/
```

### 5. Создание Pull Request

1. Убедитесь, что все тесты проходят
2. Обновите документацию если необходимо
3. Создайте Pull Request с описанием изменений
4. Дождитесь проверки CI/CD

## Типы вкладов

- 🐛 **Исправление багов**: Исправление ошибок в коде
- ✨ **Новые функции**: Добавление нового функционала
- 📚 **Документация**: Улучшение документации
- 🧪 **Тесты**: Добавление или улучшение тестов
- 🎨 **Стиль**: Улучшение форматирования и стиля кода

## Соглашение о коммитах

Используйте понятные сообщения коммитов:

- `feat: добавить поддержку новых команд`
- `fix: исправить обработку таймаутов API`
- `docs: обновить README.md`
- `test: добавить тесты для функции X`

## Сообщение о проблемах

Если вы нашли баг или хотите предложить новую функцию:

1. Проверьте существующие issues
2. Создайте новый issue с подробным описанием
3. Укажите версию Python, ОС и шаги для воспроизведения

## Кодекс поведения

Этот проект придерживается принципа уважительного общения. Будьте вежливы и конструктивны в общении.

## Лицензия

Внося вклад в проект, вы соглашаетесь с тем, что ваш вклад будет опубликован под лицензией MIT.
